@model CourseOPR.Models.Speciality

@{
    ViewData["Title"] = "Create";
}

<h1>Создать</h1>

<h4>Специальности</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SpecialityName" class="control-label">Специальность</label>
                <input asp-for="SpecialityName" class="form-control" />
                <span asp-validation-for="SpecialityName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FacultyId" class="control-label">Факультет</label>
                <select asp-for="FacultyId" class ="form-control" asp-items="ViewBag.FacultyName"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Создать" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад к списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
